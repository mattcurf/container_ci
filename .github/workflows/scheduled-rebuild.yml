name: Scheduled Rebuild

on:
  workflow_dispatch:  # Rebuild on demand only

jobs:
  rebuild:
    uses: ./.github/workflows/build-publish.yml
    permissions:
      contents: read
      packages: write
      id-token: write
      security-events: write
